# Use the official PHP image with Apache
FROM php:8.0-apache

# Copy custom PHP code to the Apache web directory
COPY index.php /var/www/html/

# Expose port 80
EXPOSE 80

# The Apache server is already set up to run, so no need to specify CMD
